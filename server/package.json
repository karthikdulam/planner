{
  "name": "prep-atlas-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local-only API for Prep Atlas. No auth by design — bind to localhost only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "mongodb": "^6.10.0"
  }
}
